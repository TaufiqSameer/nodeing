<% if (errors && errors.length > 0) { %>
    <div class="error-box">
      <p><strong>Error!</strong></p>
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
        <% }) %>
      </ul>
    </div>
  <% } %>
  